/**
 * TODO: Bring these functions back!
 */
describe('util.arrayutils', function likethis() {
	/*
		it('should remove two elements by index', () => {
			let array = [1, 2, 3, 4, 5];
			remove(array, 1, 2);
			expect([1, 4, 5].every((n, i) => array[i] === n)).toBe(true);
		});

		it('should remove one object member by reference', () => {
			let object = {};
			let array = [1, 2, object];
			remove(array, array.indexOf(object));
			expect(array.length).toBe(2);
			expect(array.indexOf(object)).toBe(-1);
		});

		it('should remove one string member by copy', () => {
			let array = [1, 2, 'test'];
			remove(array, array.indexOf('test'));
			expect(array.length).toBe(2);
			expect(array.indexOf('test')).toBe(-1);
		});

		it('should remove from one reference up to another', () => {
			let array = [1, 2, 'start', 'nuke', 'nuke', 'stop', 3, 4];
			remove(array, array.indexOf('start'), array.indexOf('stop'));
			expect([1, 2, 3, 4].every((n, i) => array[i] === n)).toBe(true);
		});
		*/
});
